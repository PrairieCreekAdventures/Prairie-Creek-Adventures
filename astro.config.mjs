import { defineConfig } from 'astro/config';

export default defineConfig({
  // Update this to your actual domain once you have it set up
  site: 'https://prairiecreekfarms.com',
  build: {
    format: 'directory'
  }
});
